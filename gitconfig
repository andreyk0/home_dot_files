[core]
  excludesfile = ~/.gitignore
	editor = vim

[color]
  diff = auto
  branch = auto
  status = auto

[user]
  # export GIT_AUTHOR_NAME="Andrey Kartashov"
  # export GIT_AUTHOR_EMAIL="andrey.kartashov@gmail.com"
  # export GIT_COMMITTER_NAME="$GIT_AUTHOR_NAME"
  # export GIT_COMMITTER_EMAIL="$GIT_AUTHOR_EMAIL"

[push]
  default = tracking

[alias]
  #datetag = !git tag `date \"+%Y-%m-%d-%H%M%S\"`
  #rctag = !git tag `date \"+%Y-%m-%d-%H%M%S\"`-rc
  #livetag = !git tag `date \"+%Y-%m-%d-%H%M%S\"`-live

  rtag = !git tag r`date \"+%Y-%m-%d-%H%M%S\"`
  st = !git status
  up = ! git checkout master && git pull && git fetch --tags -p && git-delete-merged-branches && git status
  ci = !git commit
  wdiff = !git diff --color-words
  lg = !git log --graph
  lgg = !git log --graph --pretty='format:"%C(yellow)%h %Creset%C(cyan)%G? %GK%Creset %C(green)%ae%Creset %<(80,trunc)%s"'

[diff]
  tool = vimdiff
	ignoreSubmodules = dirty


[commit]
	gpgsign = true
  template = ~/.git_commit_template

#
#
# git config core.ignorecase false
#
#
